¬иртуальна€ машина дл€ веб-разработки
----------

¬иртуальна€ машина основата на утилите Vagrant, дл€ работы нужно следующее:
 - [Vagrant](http://www.vagrantup.com/downloads.html)
 - [VirtualBox](https://www.virtualbox.org/wiki/Downloads)

ѕосле развертывани€, запускаетс€ из консоли командой `vagrant up`. 
ѕосле этого начинаетс€ процесс инициализации, который длитс€ достаточно долго, инициализаци€ производитс€ только при первом запуске.
¬о врем€ инициализации из интернета скачиваетс€ некоторое количество мегабайт, так что нужен неплохой интернет.
ѕодробнее о работе с утилитой можно почитать [здесь](http://habrahabr.ru/post/113354/). ќписание установки и настройки можно пропустить.

ѕосле окончани€ работы машину нужно корректно выключить командой `vagrant halt`, доступ к виртуальному серверу можно получить с помощью `vagrant ssh` или напр€мую - адрес машины `192.168.2.10`.

—ервер предоставл€ет следующие сервисы:
 - ¬еб-сервер (Apache 2 + PHP 5.4),
 - MySQL
 - PostgreSQL
 - Redis
 - Memcached
 - DNS
 
“ак же внутри настроены SQLite, Vim, Python PIP, PHP Pear и другие при€тные вещи.


”тилита WinHosts
-----------

¬иртуальный сервер автоматически определ€ет DocumentRoot по заголовку Host, используетс€ принцип `(?P<document_root>.+?)\.loc`.
ѕо умолчанию он ищет сайты в каталоге `D:\htdocs`, это можно изменить в файле `Params.rb`.
„тобы браузер в Windows корректно открывал сайты в зоне .loc, можно указать в качестве DNS-сервера `192.168.2.10`, но это может (и скорее всего так и будет) привести к замедлению работы интернета.

јльтернативой этому €вл€етс€ использование утилиты WinHosts, котора€ просматривает папку `D:\htdocs` и дописывает все нужные домены в файл `hosts`.

¬ случае, если утилита не сможет записать файл самосто€тельно (в Windows 8.1 это всегда так, даже с правами администратора), 
она покажет нужный контент в блокноте, в этом случае нужно скопировать нужные значени€ вручную в файл `hosts`.
